# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppList' do
  use_frameworks!
	pod 'Alamofire', '~> 4.3'
	pod 'SwiftyJSON'
	pod 'ALLoadingView', '~> 1.1'
    pod 'ObjectMapper'
end
